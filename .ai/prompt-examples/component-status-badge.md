@CLAUDE.md

## Task

Build a StatusBadge component located at shared/components/status-badge.tsx

## Props

status: "approved" | "pending" | "rejected" | "cancelled"
className?: string

## Requirements

- Pill shape with semantic color per status
- Uses bg-success-light/text-success pattern from design system
- Displays Thai label per status
- No click behavior — presentational only

## DO NOT

- Add state unless explicitly needed
- Install new packages
- No any type
- No relative imports
- No cross-feature imports

## Output

Show component interface first (props + structure).
Wait for my approval before writing code.
